
name: CI/CD Pipeline

on:
  push:
    branches:
      - dev

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      services: ${{ steps.filter.outputs.services }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # 🔥 Asegura que git diff funcione correctamente

      - name: Get changed files
        id: filter
        run: |
          # Obtener lista de archivos modificados
          changed_files=$(git diff --name-only ${{ github.event.before }} ${{ github.sha }})

          echo "🔍 Archivos modificados detectados:"
          echo "$changed_files"

          # Extraer solo los nombres de los microservicios modificados (ejemplo: UserDomain/CreateUserService)
          services=$(echo "$changed_files" | awk -F'/' '{print $1 "/" $2}' | sort -u | uniq)

          echo "📌 Microservicios detectados para reconstrucción: $services"

          # Asegurar que siempre haya una salida JSON válida
          if [[ -z "$services" ]]; then
            echo "⚠ No hay microservicios modificados."
            echo "services=[]" >> $GITHUB_OUTPUT
          else
            json_output=$(echo "$services" | jq -R -s -c 'split("\n") | map(select(length > 0))')
            echo "services=$json_output" >> $GITHUB_OUTPUT
          fi

  build-and-push:
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.services != '[]' }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: ${{ fromJson(needs.detect-changes.outputs.services) }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          if [[ -z "${{ matrix.service }}" ]]; then
            echo "Error: SERVICE is empty. Skipping build."
            exit 1
          fi
          
          SERVICE=${{ matrix.service }}
          IMAGE_NAME="fernanyag20/${SERVICE,,}"
          
          echo "📌 Building Docker image for $SERVICE..."
          
          # 🔥 Determinar el path del microservicio (ejemplo: UserDomain/CreateUserService)
          BUILD_PATH="$SERVICE"

          if [[ ! -f "$BUILD_PATH/Dockerfile" ]]; then
            echo "❌ ERROR: Dockerfile not found in $BUILD_PATH/"
            exit 1
          fi
          
          docker build -t $IMAGE_NAME:latest $BUILD_PATH
          
          echo "🚀 Pushing Docker image to DockerHub..."
          docker push $IMAGE_NAME:latest


  deploy-to-ec2:
    needs: build-and-push
    if: ${{ needs.detect-changes.outputs.services != '[]' }}  # Solo se ejecuta si hay cambios
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: ${{ fromJson(needs.detect-changes.outputs.services) }}
    steps:
      - name: Trigger redeploy on EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST_DNS }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            if [[ -z "${{ matrix.service }}" ]]; then
              echo "Error: SERVICE is empty. Skipping deployment."
              exit 1
            fi
            SERVICE=${{ matrix.service }}
            IMAGE_NAME="fernanyag20/${SERVICE,,}"

            docker pull $IMAGE_NAME:latest
            docker stop $SERVICE || true
            docker rm $SERVICE || true
            docker run -d --name $SERVICE --restart always -p 80:80 -e JWT_SECRET_KEY="${{ secrets.JWT_SECRET_KEY }}" $IMAGE_NAME:latest
            
            echo "Desplegado $SERVICE en ${{ secrets.HOST_DNS }}"
